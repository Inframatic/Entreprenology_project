<h1>Startups Index</h1>
<%= link_to "New Startup", "/startups/new" %>

<% @startups.each do |startup| %>
  <p><%= startup[:company_name] %>, <%= startup[:number_of_employees] %>,
  <img src="<%= startup[:logo] %>">
<% end %>